#include <stdio.h>

int main()
{
   int  t, n, k;
   scanf("%d", &t);
   while (t--)
   {
        scnaf("%d%d", &n, &k);
        int arr[k][n], prev = 0, new = 0;
        int i = 0, j = 0;
        while (i < k)
        {
            j = 0;
            while (j < n)
            {
                scanf("%d", &arr[i][j]);
                j++;
            }
        }
        i = 0;
        while (i < n)
        {
            j = 0;
            while (j < k)
            {
                if ()
            }
        }


   }
   return (0);
}